package org.krakenapps.pcap.decoder.smb.ntresp;

import org.krakenapps.pcap.decoder.smb.TransData;

public class NtTransactSetSecurityDescResponse implements TransData{
	// does not exist neither parameter nor data
}
